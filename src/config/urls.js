const baseURL = 'http://jsonplaceholder.typicode.com';

const urls = {
    users: '/users',
    posts: '/posts',
    comments: '/comments',
    albums: '/albums',
    photos: '/photos',
    todos: '/todos'
}

export {
    baseURL,
    urls
}